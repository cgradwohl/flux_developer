apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: default

configMapGenerator:
  - name: dev-beetle-config
    files:
      - global-config.yaml=../../global-config.yaml
      - service-config.yaml=service-config.yaml

options:
  disableNameSuffixHash: true
